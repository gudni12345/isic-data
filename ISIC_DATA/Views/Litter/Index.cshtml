@model IEnumerable<ISIC_DATA.Models.Litter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateOfBirth)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Breeder.Id)
        </th>
        <th>Father </th>
        <th> Mother</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfBirth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Breeder.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Father_Id.Reg)
        </td>
         <td>
            @Html.DisplayFor(modelItem => item.Mother_Id.Reg)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LitterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LitterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LitterId })
        </td>
    </tr>
}

</table>
